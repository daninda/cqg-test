import { Routes } from '@angular/router';
import { PackageListComponent } from './features/package-list/package-list.component';

export const routes: Routes = [
    {
        path: '',
        component: PackageListComponent,
    },
];
